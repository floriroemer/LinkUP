name: linkup
description: LinkUP - a local-first P2P messenger prototype.
publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.6
  file_picker: ^10.3.2
  google_fonts: ^6.3.2
  qr_flutter: ^4.1.0
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true